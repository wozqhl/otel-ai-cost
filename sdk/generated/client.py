# Auto-generated by sdk-mcp-gen — do not edit by hand
# API: OTel AI Cost Local Serve API
from __future__ import annotations

import json
import re
import urllib.error
import urllib.parse
import urllib.request
from typing import Any, Mapping, MutableMapping, Optional

__package_name__ = "client"

class Client:
    """Sync HTTP client stub generated from OpenAPI."""

    def __init__(self, base_url: str = "", opener: Any = None) -> None:
        self.base_url = (base_url or "").rstrip("/")
        self._opener = opener

    def _request(self, method: str, path: str, body: Any = None) -> Any:
        url = self.base_url + path
        data = None
        headers: dict[str, str] = {}
        if body is not None:
            data = json.dumps(body).encode("utf-8")
            headers["Content-Type"] = "application/json"
        req = urllib.request.Request(url, data=data, headers=headers, method=method)
        open_url = self._opener.open if self._opener is not None else urllib.request.urlopen
        try:
            with open_url(req) as res:
                text = res.read().decode("utf-8")
                return json.loads(text) if text else None
        except urllib.error.HTTPError as e:
            raise RuntimeError(f"{method} {path} -> {e.code}") from e

    def getHealth(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Health check"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/health"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getReady(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Readiness (503 while shutting down)"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/ready"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getIndex(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """HTML cost report"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getReport(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Structured cost report JSON"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/report.json"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getCostsCsv(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Finance CSV cost export"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/v1/costs.csv"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getCostsMd(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """FinOps Markdown cost report"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/v1/costs.md"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getCostsGha(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """GitHub Actions annotations alias (same as GET /v1/costs?format=gha)"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/v1/costs.gha.txt"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getCosts(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Cost snapshot (json | csv | md | gha)"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/v1/costs"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getBudgets(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Configured budget thresholds (not spend)"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/v1/budgets"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getModels(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Pricing catalog (rates, not spend)"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/v1/models"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def listSpans(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Recent span summaries (FinOps debug)"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/v1/spans"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def listTenants(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Per-tenant cost rollup (actual spend)"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/v1/tenants"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def postTraces(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Ingest OTLP JSON traces"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/v1/traces"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        return self._request("POST", path, dict(args_map))

    def postTracesAlias(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Ingest OTLP JSON traces (alias)"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/v1/otlp/v1/traces"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        return self._request("POST", path, dict(args_map))

    def getMetrics(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Prometheus metrics"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/metrics"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getOpenApi(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """OpenAPI 3 document"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/openapi.json"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))

    def getConfig(self, args: Optional[Mapping[str, Any]] = None) -> Any:
        """Redacted runtime config"""
        args_map: MutableMapping[str, Any] = dict(args or {})
        path = "/v1/config"
        def _sub(m: Any) -> str:
            key = m.group(1)
            val = args_map.pop(key, "")
            return urllib.parse.quote(str(val), safe="")
        path = re.sub(r"\{([^}]+)\}", _sub, path)
        q = {k: str(v) for k, v in args_map.items() if v is not None}
        qs = urllib.parse.urlencode(q)
        return self._request("GET", path + ("?" + qs if qs else ""))


def create_client(base_url: str = "", opener: Any = None) -> Client:
    return Client(base_url=base_url, opener=opener)
