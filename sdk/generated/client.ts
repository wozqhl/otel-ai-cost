// Auto-generated by sdk-mcp-gen — do not edit by hand
// API: OTel AI Cost Local Serve API
// package: client
export type Json = null | boolean | number | string | Json[] | { [k: string]: Json };
export interface ClientOptions { baseUrl?: string; fetchImpl?: typeof fetch }
export function createClient(opts: ClientOptions = {}) {
  const baseUrl = (opts.baseUrl || "").replace(/\/$/, "");
  const f = opts.fetchImpl || fetch;
  async function request(method: string, path: string, body?: unknown) {
    const res = await f(baseUrl + path, {
      method,
      headers: body ? { "content-type": "application/json" } : undefined,
      body: body ? JSON.stringify(body) : undefined,
    });
    if (!res.ok) throw new Error(method + " " + path + " -> " + res.status);
    const text = await res.text();
    return text ? JSON.parse(text) : null;
  }
  async function getHealth(args: Record<string, unknown> = {}) {
    let path = "/health";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getReady(args: Record<string, unknown> = {}) {
    let path = "/ready";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getIndex(args: Record<string, unknown> = {}) {
    let path = "/";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getReport(args: Record<string, unknown> = {}) {
    let path = "/report.json";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getCostsCsv(args: Record<string, unknown> = {}) {
    let path = "/v1/costs.csv";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getCostsMd(args: Record<string, unknown> = {}) {
    let path = "/v1/costs.md";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getCostsGha(args: Record<string, unknown> = {}) {
    let path = "/v1/costs.gha.txt";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getCosts(args: Record<string, unknown> = {}) {
    let path = "/v1/costs";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getBudgets(args: Record<string, unknown> = {}) {
    let path = "/v1/budgets";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getModels(args: Record<string, unknown> = {}) {
    let path = "/v1/models";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function listSpans(args: Record<string, unknown> = {}) {
    let path = "/v1/spans";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function listTenants(args: Record<string, unknown> = {}) {
    let path = "/v1/tenants";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function postTraces(args: Record<string, unknown> = {}) {
    let path = "/v1/traces";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    return request("POST", path, args);
  }
  async function postTracesAlias(args: Record<string, unknown> = {}) {
    let path = "/v1/otlp/v1/traces";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    return request("POST", path, args);
  }
  async function getMetrics(args: Record<string, unknown> = {}) {
    let path = "/metrics";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getOpenApi(args: Record<string, unknown> = {}) {
    let path = "/openapi.json";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getConfig(args: Record<string, unknown> = {}) {
    let path = "/v1/config";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  return {
    getHealth,
    getReady,
    getIndex,
    getReport,
    getCostsCsv,
    getCostsMd,
    getCostsGha,
    getCosts,
    getBudgets,
    getModels,
    listSpans,
    listTenants,
    postTraces,
    postTracesAlias,
    getMetrics,
    getOpenApi,
    getConfig,
  };
}
